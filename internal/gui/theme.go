package gui

// Fyne theme size constants for premium dark design.
const (
	sizePadding            = 8 // Compact, modern breathing room
	sizeInnerPadding       = 5 // Tighter inner spacing for high item density
	sizeScrollBar          = 6 // Slimmer, modern scrollbars
	sizeSeparatorThickness = 1
	sizeInputBorder        = 1
	sizeInputRadius        = 8  // Clean, modern inputs
	sizeCardRadius         = 10 // Beautiful card rounded corners
	sizeButtonRadius       = 8  // Rounded buttons

	// Quick Access detail-view margins (breathing room so
	// content never touches the window border).
	detailHPad = 12 // horizontal inset for header, action rows, footer
	detailVPad = 10 // vertical inset for the header card
)
